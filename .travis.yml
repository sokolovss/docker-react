sudo: required
services: 
      - docker
before_install:
        - docker build -t sokolovs/docker-react -f Dockerfile.dev .

script:
        - docker run -e CI=true sokolovs/docker-react npm run test
